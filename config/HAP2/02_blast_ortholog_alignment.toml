# ============================================================================
# HAP2 — Gene-Specific BLAST Query FASTAs
# ============================================================================
# Only query FASTAs are unique per gene group.
# Shared defaults (e-values, word sizes, targets, flags) are in
# b_blast_alignmentCONFIG.toml (project root).
# ============================================================================
# TODO: Add HAP2 query FASTA paths once input files are prepared in
#       II_INPUTS/HAP2_query_fasta_file/ (or similar directory).
# Example:
#   query_fastas = [
#       "II_INPUTS/HAP2_query_fasta_file/Species_name/HAP2_nuc_seq.fasta",
#   ]
#   query_protein_fastas = [
#       "II_INPUTS/HAP2_query_fasta_file/Species_name/HAP2_prot_seq.fasta",
#   ]
# ============================================================================

[ortholog_blast]
query_fastas = []
query_protein_fastas = []
